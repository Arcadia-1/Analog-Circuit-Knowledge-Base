---
import Base from '../../layouts/Base.astro';
import ReadingTheBits from '../../illustrations/bits/ReadingTheBits.svelte';
import '../../styles/illustration.css';
---
<Base
  title="Recovering SAR bit weights"
  description="Clip, offset, or break a bit of a binary, redundant or sub-radix SAR converter, and read its raw output the way ADCToolbox does: bit activity, segment residues, calibrated weights and their radix, and ENOB bit by bit."
>
  <ReadingTheBits client:load />
</Base>

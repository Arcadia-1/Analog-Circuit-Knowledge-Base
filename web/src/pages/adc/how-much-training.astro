---
import Base from '../../layouts/Base.astro';
import HowMuchTraining from '../../illustrations/training/HowMuchTraining.svelte';
import '../../styles/illustration.css';
---
<Base
  title="Choosing calibration record length"
  description="Foreground weight calibration of a mismatched SAR: how many training samples before the weights work on a capture they have never seen, why a short record can look perfect on its own data, and why redundancy makes an array teachable."
>
  <HowMuchTraining client:load />
</Base>

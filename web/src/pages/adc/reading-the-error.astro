---
import Base from '../../layouts/Base.astro';
import ReadingTheError from '../../illustrations/errors/ReadingTheError.svelte';
import '../../styles/illustration.css';
---
<Base
  title="Reading the error"
  description="Fit a sine to an ADC's output, subtract it, and read the residual four ways — against the value, against the phase, as a distribution and as a spectrum — to tell noise, static curvature, jitter, memory and interference apart."
>
  <ReadingTheError client:load />
</Base>
